import 'package:flutter/material.dart';

class RetutnTab extends StatelessWidget {
  const RetutnTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const [],
    );
  }
}
